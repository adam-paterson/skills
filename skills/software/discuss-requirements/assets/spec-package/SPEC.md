---
id: {{SPEC_ID}}
title: "{{TITLE}}"
status: draft
created: {{CREATED}}
updated: {{UPDATED}}
---

# {{TITLE}}

This file is initialized by `discuss-requirements` so the package is complete. Fill it with `spec-generation` after requirements are ready.

## Requirements Source

- [Requirements](./REQUIREMENTS.md)

## Problem

Fill in during `spec-generation`.

## Goal

Fill in during `spec-generation`.

## Non-Goals

Fill in during `spec-generation`.

## Current State

Fill in during `spec-generation`.

## Target Behavior

Fill in during `spec-generation`.

## Public Interfaces

Fill in during `spec-generation`.

## Data Flow

Fill in during `spec-generation`.

## Failure Modes

Fill in during `spec-generation`.

## Compatibility

Fill in during `spec-generation`.

## Open Questions

Carry forward unresolved questions from `REQUIREMENTS.md`.
